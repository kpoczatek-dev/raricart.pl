# Protect config.json from direct access
<Files "config.json">
    Order Deny,Allow
    Deny from all
</Files>

# Allow public access to content and status for JS
<FilesMatch "^(content|status|gallery)\.json$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
